import 'package:flutter/material.dart';

class ChestPage extends StatelessWidget {
  const ChestPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Chest Page", style: TextStyle(fontSize: 40))),
    );
  }
}
