import 'package:flutter/material.dart';

class ArmPage extends StatelessWidget {
  const ArmPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("ARM Page", style: TextStyle(fontSize: 40))),
    );
  }
}
